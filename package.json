{
  "name": "cheapmate",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx --yes serve . --no-clipboard -l tcp://0.0.0.0:${PORT:-8080}",
    "dev": "npx --yes serve . -l 8080",
    "build": "echo 'Nothing to build - plain HTML/JS/WASM.'",
    "test": "node tools/uitest.mjs",
    "mate": "node tools/mateinstinct.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
